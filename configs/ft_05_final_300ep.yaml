# Final, best config
# Ablation ladder row 5 (pretrained) — docs/HPARAMS.md section 4.
#
#   python train.py --config configs/ft_05_final_300ep.yaml
#
# Anything omitted comes from the "pretrained" recipe. Flags still override this
# file: `--config configs/ft_05_final_300ep.yaml --epochs 300`.
#
# NOTE: row 5 is 'best config' — it sets the 300-epoch budget only; carry the
# NOTE: winning architecture flags yourself.

recipe: "pretrained"
epochs: 300
