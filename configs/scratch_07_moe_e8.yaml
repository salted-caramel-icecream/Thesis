# N=8, last stage
# Ablation ladder row 7 (scratch) — docs/HPARAMS.md section 4.
#
#   python train.py --config configs/scratch_07_moe_e8.yaml
#
# Anything omitted comes from the "scratch" recipe. Flags still override this
# file: `--config configs/scratch_07_moe_e8.yaml --epochs 300`.

recipe: "scratch"
epochs: 90
model:
  moe:
    num_experts: 8
    shared_expert: true
  ablation:
    use_moe: true
    moe_placement: [[], [], [], [-1]]
