# MoE + shared
# Ablation ladder row 4 (scratch) — docs/HPARAMS.md section 4.
#
#   python train.py --config configs/scratch_04_moe_shared.yaml
#
# Anything omitted comes from the "scratch" recipe. Flags still override this
# file: `--config configs/scratch_04_moe_shared.yaml --epochs 300`.

recipe: "scratch"
epochs: 90
model:
  moe:
    num_experts: 4
    shared_expert: true
  ablation:
    use_moe: true
    moe_placement: [[], [], [], [1]]
