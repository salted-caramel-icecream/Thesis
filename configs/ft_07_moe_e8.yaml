# N=8, last stage
# Ablation ladder row 7 (pretrained) — docs/HPARAMS.md section 4.
#
#   python train.py --config configs/ft_07_moe_e8.yaml
#
# Anything omitted comes from the "pretrained" recipe. Flags still override this
# file: `--config configs/ft_07_moe_e8.yaml --epochs 300`.

recipe: "pretrained"
epochs: 100
model:
  moe:
    num_experts: 8
    shared_expert: true
  seed_moe_from_dense: true
  ablation:
    use_moe: true
    moe_placement: [[], [], [], [-1]]
