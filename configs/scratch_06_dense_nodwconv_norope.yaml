# Dense, no DWConv, no RoPE
# Ablation ladder row 6 (scratch) — docs/HPARAMS.md section 4.
#
#   python train.py --config configs/scratch_06_dense_nodwconv_norope.yaml
#
# Anything omitted comes from the "scratch" recipe. Flags still override this
# file: `--config configs/scratch_06_dense_nodwconv_norope.yaml --epochs 300`.

recipe: "scratch"
epochs: 90
model:
  dense_dwconv: false
  ablation:
    use_moe: false
    use_rope: false
