# Dense, fine-tuned, no MoE
# Ablation ladder row 2 (pretrained) — docs/HPARAMS.md section 4.
#
#   python train.py --config configs/ft_02_dense_finetune.yaml
#
# Anything omitted comes from the "pretrained" recipe. Flags still override this
# file: `--config configs/ft_02_dense_finetune.yaml --epochs 300`.
#
# NOTE: the spec names only 'no MoE' for this row; dense_dwconv and use_rope
# NOTE: stay at your flags/defaults. For 2->3 to isolate MoE alone, match them
# NOTE: to your MoE runs.

recipe: "pretrained"
epochs: 100
model:
  ablation:
    use_moe: false
